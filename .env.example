# Server-side AI proxy (recommended). The key never reaches the browser.
ANTHROPIC_API_KEY=sk-ant-...
# Optional: override the default model used by the proxy.
ANTHROPIC_MODEL=claude-opus-5
# Port for the local API server (Vite proxies /api to it in dev).
PORT=8787
